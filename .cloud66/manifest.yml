production:
    rails:
        configuration:
            nginx:
                cors:
                    origin: '*'
                    methods: 'GET, OPTION'
    postgresql:
        server:
            unique_name: app
            root_disk_size: 100
        configuration:
            root_disk_size: 60
